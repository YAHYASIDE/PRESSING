/* services/index.js — use-case layer (scaffold).
 *
 * Use-cases orchestrate core (pure rules) + repositories (persistence) on behalf
 * of pages. They are extracted from bindScreen() in a later commit (see
 * docs/MIGRATION.md, commit 5). For now this file only reserves the layer so the
 * load order and folder structure are in place. It publishes nothing yet.
 */
(function (App) {
  "use strict";
  // App.services.<useCase> = function (...) { ... }  ← added incrementally later.
})(window.App);
